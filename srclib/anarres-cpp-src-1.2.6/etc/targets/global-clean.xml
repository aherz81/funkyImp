<?xml version="1.0" encoding="UTF-8"?>

<project name="global-clean" basedir=".">
	<target name="global-clean">
		<delete dir="${global.dir.root}/build" />
		<delete dir="${global.dir.root}/webroot" />
                <delete file="../../lib/${global.file.jar.name}" />
	</target>
</project>
